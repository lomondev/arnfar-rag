import { LessonsReviewClient } from "@/features/learn/LessonsReviewClient";

export const metadata = { title: "Studio · Lessons — Arnfar AI" };

export default function LessonsPage() {
  return <LessonsReviewClient />;
}
