import { ExportClient } from "@/features/dataset/ExportClient";

export const metadata = { title: "Studio · Export — Arnfar AI" };

export default function ExportPage() {
  return <ExportClient />;
}
