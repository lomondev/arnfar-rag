import { AccountsClient } from "@/features/dataset/AccountsClient";

export const metadata = { title: "Studio · Accounts — Arnfar AI" };

export default function AccountsPage() {
  return <AccountsClient />;
}
