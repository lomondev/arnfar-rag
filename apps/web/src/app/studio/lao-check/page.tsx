import { LaoCheckClient } from "@/features/lao/LaoCheckClient";

export const metadata = { title: "Studio · Lao check — Arnfar AI" };

export default function LaoCheckPage() {
  return <LaoCheckClient />;
}
