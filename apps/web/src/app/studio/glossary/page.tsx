import { GlossaryClient } from "@/features/dataset/GlossaryClient";

export const metadata = { title: "Studio · Glossary — Arnfar AI" };

export default function GlossaryPage() {
  return <GlossaryClient />;
}
