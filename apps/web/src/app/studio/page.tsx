import { DashboardClient } from "@/features/dashboard/DashboardClient";

export const metadata = { title: "Studio · Overview — Arnfar AI" };

export default function StudioHomePage() {
  return <DashboardClient />;
}
