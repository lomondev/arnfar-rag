{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // "bun" for the test files' `bun:test` import. The runtime schemas themselves stay
    // environment-agnostic — they are consumed by Next.js in the browser too.
    "types": ["bun"],
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"]
}
